[project]
name = "continuous-auth-integrations"
version = "0.1.0"
description = "Shared external integrations for the Continuous Authentication platform."
requires-python = ">=3.13"
dependencies = [
  "continuous-auth-domain",
  "continuous-auth-schemas",
  "httpx",
]

[tool.uv]
package = false
