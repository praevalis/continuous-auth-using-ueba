# Seed Data Scaffold

This directory is reserved for demo seed assets and replay fixtures.

Expected future contents may include:

- SQL seed files
- JSON fixtures
- event replay payloads
- tenant bootstrap data
